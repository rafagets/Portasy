<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rafael.portasy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ColetaDadosActivity" />
        <activity android:name=".MainMenuActivity" />
        <activity android:name=".DetalheMoradorActivity" />
        <activity android:name=".NotificarFamilhaActivity" />
        <activity android:name=".LigacaoActivity" />
        <activity android:name=".VerificaAutorizacaoActivity" />
        <activity android:name=".LiberarEntradaActivity" />
        <activity android:name=".QRCodeActivity" />
        <activity android:name=".LiberarSaidaActivity"></activity>
    </application>

</manifest>